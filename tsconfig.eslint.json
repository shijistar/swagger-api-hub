{
  "extends": "./tsconfig.json",
  "compilerOptions": {
    "baseUrl": ".",
    // ensure that nobody can accidentally use this config for a build
    "noEmit": true
  },
  "include": ["src", "test"]
}
